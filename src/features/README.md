# Feature Modules

Create one folder per domain feature:

```
src/features/<feature-name>/
```

Only add folders needed for that feature:

- `api`
- `assets`
- `components`
- `hooks`
- `stores`
- `types`
- `utils`
